// useTimer.js
// Hook customizado para cronômetro de jogos.
// Controla contagem regressiva, pausar, reiniciar e callback de timeout.

// Implementação futura.
export const useTimer = () => {
  return {
    timeLeft: 0,
    isRunning: false,
    start: () => {},
    pause: () => {},
    reset: () => {},
  };
};
