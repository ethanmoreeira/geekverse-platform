// useGameScore.js
// Hook customizado para gerenciar pontuação de jogos.
// Controla score, incremento, reset e best score via localStorage.

// Implementação futura.
export const useGameScore = () => {
  return {
    score: 0,
    bestScore: 0,
    addScore: () => {},
    resetScore: () => {},
  };
};
