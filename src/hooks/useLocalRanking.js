// useLocalRanking.js
// Hook customizado para ranking local via localStorage.
// Salva e recupera melhores pontuações por jogo.

// Implementação futura.
export const useLocalRanking = () => {
  return {
    ranking: [],
    addEntry: () => {},
    getRanking: () => [],
    clearRanking: () => {},
  };
};
