// useApi.js
// Hook customizado para chamadas de API com loading e erro.
// Abstrai fetch/axios com estados de carregamento e tratamento de erros.

// Implementação futura.
export const useApi = () => {
  return {
    data: null,
    loading: false,
    error: null,
    fetchData: () => {},
  };
};
