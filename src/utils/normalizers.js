// normalizers.js
// Utilitários para normalizar dados vindos das APIs.
// Padroniza formatos diferentes entre as 5 APIs.

// Implementação futura.
export const normalizeCharacter = (raw, source) => {
  return { ...raw, source };
};
