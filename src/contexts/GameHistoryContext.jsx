// GameHistoryContext.jsx
// Contexto para histórico de partidas jogadas.
// Armazena resultados, pontuações e estatísticas de cada jogo.

import { createContext } from 'react';

export const GameHistoryContext = createContext(null);

// GameHistoryProvider será implementado futuramente.
