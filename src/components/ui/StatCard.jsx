// StatCard.jsx
// Card de estatística para exibir métricas de jogos.
// Mostra valor numérico, rótulo e ícone opcional.

const StatCard = ({ label: _label, value: _value, icon: _icon }) => {
  return null; // Implementação futura
};

export default StatCard;
