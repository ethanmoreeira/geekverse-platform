// ModeSelector.jsx
// Seletor de modo de jogo: single player ou multiplayer local.
// Usado em jogos que suportam dois modos de jogo.

const ModeSelector = ({ onSelect: _onSelect }) => {
  return null; // Implementação futura
};

export default ModeSelector;
