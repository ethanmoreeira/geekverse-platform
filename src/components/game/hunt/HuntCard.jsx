// HuntCard.jsx
// Card individual de personagem na grade de caçada.
// Mostra imagem, nome e estado (correto/errado).

const HuntCard = ({ character, onClick }) => {
  return null; // Implementação futura
};

export default HuntCard;
