// GameControls.jsx
// Controles gerais de jogo: iniciar, pausar, reiniciar, voltar.
// Reutilizável em todos os jogos.

const GameControls = ({ onStart, onPause, onRestart }) => {
  return null; // Implementação futura
};

export default GameControls;
