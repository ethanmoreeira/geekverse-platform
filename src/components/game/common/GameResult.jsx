// GameResult.jsx
// Tela de resultado exibida ao final de cada partida.
// Mostra pontuação final, tempo, acertos e opções de replay.

const GameResult = ({ result }) => {
  return null; // Implementação futura
};

export default GameResult;
