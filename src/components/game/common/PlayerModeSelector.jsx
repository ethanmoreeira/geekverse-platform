// PlayerModeSelector.jsx
// Seletor de modo de jogadores: single player ou multiplayer local.
// Usado dentro dos jogos que suportam dois modos.

const PlayerModeSelector = ({ onSelect }) => {
  return null; // Implementação futura
};

export default PlayerModeSelector;
