// RoundIndicator.jsx
// Indicador de rodada atual em jogos com múltiplas rodadas.
// Mostra rodada atual e total de rodadas.

const RoundIndicator = ({ current, total }) => {
  return null; // Implementação futura
};

export default RoundIndicator;
