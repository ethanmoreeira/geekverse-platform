// ScorePanel.jsx
// Painel de pontuação exibido durante os jogos.
// Mostra pontuação atual, recorde e estatísticas da partida.

const ScorePanel = ({ score, bestScore }) => {
  return null; // Implementação futura
};

export default ScorePanel;
