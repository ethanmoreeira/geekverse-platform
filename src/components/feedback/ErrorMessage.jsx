// ErrorMessage.jsx
// Componente para exibir mensagens de erro amigáveis ao usuário.
// Usado em tratamento de erros de API e validações.

const ErrorMessage = ({ message: _message }) => {
  return null; // Implementação futura
};

export default ErrorMessage;
