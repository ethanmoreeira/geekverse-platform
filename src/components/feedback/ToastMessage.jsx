// ToastMessage.jsx
// Componente de notificação toast para feedback ao usuário.
// Exibe mensagens de sucesso, erro ou aviso temporárias.

const ToastMessage = ({ message, type }) => {
  return null; // Implementação futura
};

export default ToastMessage;
