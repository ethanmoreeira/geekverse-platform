// PageLoader.jsx
// Spinner de carregamento usando react-spinners.
// Exibido durante chamadas de API e carregamento de dados.

const PageLoader = () => {
  return null; // Implementação futura com react-spinners
};

export default PageLoader;
